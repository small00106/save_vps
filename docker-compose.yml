version: "3.8"

services:
  cloudnest:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8800:8800"
    volumes:
      - cloudnest-data:/app/data
    restart: unless-stopped

volumes:
  cloudnest-data:
